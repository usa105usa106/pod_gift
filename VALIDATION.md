# Validation — Gift Hunter v0034

## Hotfix непрерывного refresh FAST-форм — 18 сентября 2026

Разбор предоставленного production-лога подтвердил конкретную цепочку: `payment_form_refreshed` для двух `saved_id` завершился в `22:42:47 UTC`, затем событие `fast_forms_refreshed_and_frozen` остановило обслуживание форм на расстоянии 25 номеров. До фактического predecessor в `00:11:38 UTC` прошло около 88.8 минуты; worker завершился с `oldest_age_s=5330.599`, после чего preflight корректно отказал в отправке протухшей формы.

Исправление удаляет постоянный freeze и вводит встроенную двухзонную политику: дальше 50 номеров refresh выполняется через 300 секунд, а на расстоянии 50 и ближе — через 120 секунд. При входе в зону 50 выполняется полный forced refresh; stale-ready бит перепроверяется из RAM, а временно невооружённая цель может повторно вооружиться после 15-секундной паузы. Настройки старого FAST_FORM_FREEZE_DISTANCE больше не нужны для Coolify. Добавлена видимая метрика возраста FAST-форм в карточку сканера.

После изменения выполнен полный локальный regression-run:

```text
Ran 194 tests
OK
```

Также выполнены `compileall`, AST-разбор Python и YAML-разбор `docker-compose.yaml`. Живые Telegram/Stars-платежи в офлайн-проверке не выполнялись.

## Финальный аудит v0034 — проверка 18 сентября 2026

Полный локальный regression-run после смены версии и исправления только критичных payment-safety edge-cases:

```text
192 passed  # предыдущий финальный аудит до текущего hotfix
```

Дополнительно пройдены `compileall`, AST-разбор Python, asyncio debug-run без `RuntimeWarning`, YAML-разбор `docker-compose.yaml` с 6 сервисами и проверка отсутствия идентификаторов предыдущей версии.

Критичные исправления текущего аудита: повреждённый существующий `payment-submit-guard.json` теперь всегда блокирует LIVE/новый submit вместо silent-ignore; generation token guard меняется при каждой durable-мутации, поэтому token подтверждённого старого состояния не может совпасть с более поздним вооружённым guard. Живые Telegram/Stars-платежи в офлайн-проверке не выполнялись.

## Post-Stage 3 audit — фиксы 1–4, проверка 17 сентября 2026

После исправления четырёх подтверждённых дефектов выполнен полный локальный regression-run:

```text
Ran 190 tests
OK
```

Отдельные regression-проверки подтверждают: (1) при `OSError("disk full")` на post-submit `settings.json` durable guard остаётся на диске и при имитации рестарта восстанавливает payment hold + `live_upgrades=False`; (2) forced refresh одного `saved_id` не может считаться успешным за счёт ещё валидной старой формы; (3) искусственно задержанный prune `.history-*` не удерживает logging handler lock — обычный `logger.info()` проходит до снятия задержки; (4) после detach старого backup новый rollover создаёт свежий `.1`, который последующий prune старого segment не перезаписывает.

`SettingsStore.save()` также усилен `flush + fsync + atomic replace + directory fsync`, чтобы successful save действительно был достаточной точкой для удаления pre-submit guard. Live Telegram/Stars-запросы в локальной проверке не выполнялись.

## Этап 3/3 — фиксы 7–8, проверка 17 сентября 2026

После исправлений только пунктов 7–8 выполнен полный локальный regression-run текущего дерева:

```text
Ran 186 tests
OK
```

Отдельно подтверждено:

- legacy submit-path `MTProtoService.execute_upgrade()` и `Scanner._upgrade()`/`_finish_upgrade()` полностью удалён вместе с неиспользуемым `_upgrade_lock`; рабочий FAST-путь остаётся через `execute_upgrade_fast()` и `_fast_volley()`;
- source-guard запрещает возвращение удалённого одиночного production-path;
- `/log_full` фильтрует таймсерии построчно из binary stream непосредственно во staging-файл и не использует `read_text()`/`read_bytes()`/`splitlines()` для этой операции;
- разбиение больших staged-файлов выполняется ограниченными чтениями до 1 MiB без `bytearray` размером до 20 MB;
- pruning после успешной доставки также выполняется потоково через временный файл и atomic replace;
- regression-тест подтверждает, что при потоковой фильтрации старый timestamped entry и его traceback continuation удаляются, а свежий entry вместе с continuation сохраняется;
- отдельный синтетический прогон на файле ровно `25,000,000` байт дал `25,000,000` байт на выходе при пике Python-аллокаций `15,803` байта по `tracemalloc` внутри фильтра (без учёта системных буферов файловой подсистемы).

Дополнительно успешно пройдены `compileall`, AST-разбор всех Python-файлов и YAML-разбор `docker-compose.yaml`; обнаружено ровно 6 сервисов. Live Telegram/Coolify/Stars-запросы в этой локальной проверке не выполнялись.

## Этап 2/3 — фиксы 4–6, проверка 17 сентября 2026

После исправлений только пунктов 4–6 выполнен полный локальный regression-run
текущего дерева:

```text
Ran 193 tests
OK
```

Отдельно подтверждено:

- очередь фонового refresh сортируется по возрасту формы от самой старой к самой
  свежей;
- при малом запасе до safety-age и/или высокой измеренной задержке
  `getPaymentForm` batch автоматически растёт выше базового значения, но не выше
  50;
- `FloodWaitError` из прямого FAST `client(prepared.request)` записывает общий
  persistent cooldown в `rate-limit.json`, и новый `RateLimitStore` после
  повторной загрузки видит оставшееся время;
- payment-audit использует собственный handler с `handleError`, поэтому ошибка
  записи на диск больше не теряется внутри стандартного `logging`;
- периодический audit health-check выполняет запись + `flush` + `fsync` через
  `asyncio.to_thread`, а diagnostics содержит отдельный блок `payment_audit` с
  признаком `writable` и последней ошибкой;
- пункты 7–8 не изменялись.

Дополнительно успешно пройдены `compileall`, AST-разбор всех Python-файлов и
YAML-разбор `docker-compose.yaml`; обнаружено ровно 6 сервисов.

## Этап 1/3 — повторная проверка 17 сентября 2026

После фиксов 1–3 выполнен полный локальный regression-run текущего дерева:

```text
Ran 186 tests
186 passed
```

Дополнительно проверено, что pre-critical refresh обновляет весь FAST-комплект и
после этого блокирует фоновый refresh, горячий цикл `_run` больше не вызывает
`service.prepare_upgrade()`, а тяжёлая сборка/очистка `/log_full` выполняется через
`asyncio.to_thread(...)` и защищена от двух одновременных экспортов.

Проверка выполнена 5 августа 2026 года в отдельной Python 3.13 virtualenv. После создания релизного ZIP архив дополнительно распакован в новую директорию и повторно проверен из второй чистой virtualenv.

## Автоматические проверки

- `compileall` и AST-разбор всех Python-файлов;
- YAML-разбор `docker-compose.yaml`;
- ровно 6 сервисов `hunter-1` … `hunter-6`;
- 12 persistent volumes: шесть data-volume, один общий provision-volume и пять изолированных token-volume;
- отсутствие опубликованных наружу портов;
- отсутствие session/token-файлов в релизе;
- отсутствие ссылок на предыдущую версию;
- полный набор `unittest` с coverage;
- 100 000 случайных бинарных пакетов через пять UDP-декодеров;
- отдельные source-guards горячего пути;
- SHA-256 и release manifest готового архива.

```text
Ran 182 tests
OK
Coverage total: 61%
cluster.py: 83%
logic.py: 92%
main.py: 57%
```

## Критические исправления v0034

### Формы Stars при длительном ожидании и залп 50

Проверено тестами и source-guards:

- лимит залпа Hunter 1–6 равен 50; отдельный тест создаёт и запускает 50 подготовленных FAST-задач;
- `PAYMENT_FORM_MAX_AGE_SECONDS=480` меньше серверного TTL 600 секунд; refresh-порог равен 300 с дальше 50 номеров и 120 с в зоне 50 номеров и ближе;
- отдельный refresh-worker работает всё время активного LIVE и обновляет формы малыми партиями;
- тест с формой возрастом 31 день подтверждает её замену на новую до выстрела;
- новый `form_id`, invoice и request повторно валидируются для конкретного `saved_id`; duplicate `form_id` при refresh не вооружается;
- FAST fail-closed блокирует форму старше safety-age и не отправляет платёж;
- `/log_full` фильтрует основной лог, `payment-audit.jsonl`, stress-history и cluster-events по окну последних 24 часов;
- архивы больше безопасного лимита делятся на несколько самостоятельных ZIP-частей;
- локальная история старше 24 часов удаляется только после успешной отправки всех частей, а при ошибке отправки сохраняется.

### Автоматический общий FIRE_SECRET

Проверено:

- Hunter 1 без переменной Coolify создаёт 64-символьный hex-ключ в `/provision/fire-secret.txt`;
- файл имеет права `0600` и не попадает в `/log_full`;
- повторный запуск читает тот же ключ, а не меняет его;
- Hunter 2–6 читают тот же ключ и никогда не генерируют независимый;
- конкурентная генерация при rolling redeploy защищена файловой блокировкой и `O_EXCL`;
- повреждённый или частично записанный первый файл автоматически восстанавливается;
- Compose больше не требует `FIRE_SECRET`, но необязательный override остаётся совместимым.

### Локальный статус Hunter 1

Проверено, что собственный статус Hunter 1 хранится в `last_local_status`, не добавляется в `remote_statuses` и не создаёт ложное событие `shooter_outside_active_set`.

### FIRE привязан к коллекции

Подписанный пакет содержит:

```text
generation + campaign_id + shot + trigger
```

Проверено:

- FIRE другой коллекции с тем же номером игнорируется;
- FIRE другой generation игнорируется;
- неверный `trigger`, не равный `shot - 1`, не отправляется;
- пакет создаётся и подписывается при вооружении, а не в горячем пути;
- без pre-arm отправка возвращает ноль и не строит HMAC на лету.

### Первый обнаруживший не блокирует остальных

Проверен сценарий, когда первый Hunter видит точный предшественник, но его локальная форма отсутствует или устарела. Собственный платёж не стартует, однако заранее подготовленный UDP FIRE всё равно передаётся остальным участникам и фиксируется событием `fast_local_preflight_failed_relayed`.

### Реальная готовность UDP

Перед LIVE/Start в составе больше одного Hunter выполняется одноразовый подписанный PING/PONG. Docker DNS сам по себе больше не считается достаточной готовностью: каждый выбранный процесс должен ответить. Фонового постоянного Ping нет.

### Монотонная generation

Проверено восстановление поколения после потери `cluster.json` из:

- `cluster-generation.json`;
- lifecycle-файлов Hunter;
- последней корректной конфигурации в памяти.

Новая generation всегда больше максимального известного значения.

### Защита скорости

Статически и тестами подтверждено:

```text
создание всех локальных asyncio.Task
→ без await / logger / record_cluster_event / store.save
→ broadcast prebuilt UDP FIRE
```

В `broadcast_fire_nowait` отсутствуют `encode_fire` и HMAC-вычисление. FIRE/STATUS/config-notice callbacks не выполняют синхронную запись shared-volume в event loop.

Фактический send-start каждого платежа измеряется непосредственно перед:

```python
await client(prepared.request)
```

а не в момент создания задачи.

### Изоляция токенов

Hunter 2–6 используют пять отдельных read-only token-volume. Каждый вторичный контейнер видит только свой файл; Hunter 1 монтирует все пять для provisioning. Docker socket и публичный UDP-порт отсутствуют.

## Сохранённые функции

- Hunter 1: независимый залп `1–50`;
- Hunter 2–6: независимый залп `1–50` у каждого;
- точный predecessor-trigger без второго target lookup;
- локальные платёжные задачи раньше UDP FIRE;
- no app retry, post-submit verification и hold неопределённого результата;
- тихий режим, полная карточка и ручное RAM-обновление;
- ручной PONG с RAM, uptime и RTT стрелков;
- динамический каталог коллекций с current/total;
- безопасное изменение состава и watchdog;
- `/log_full` с `cluster.json`, `cluster-generation.json`, lifecycle и общим журналом.

## Ограничения среды

Автотесты используют проектные заглушки aiogram/Telethon. Production-зависимости из `requirements.txt` в проверочную virtualenv не устанавливались.

Не выполнялись:

- живой deployment в Coolify;
- реальный Bot API polling шести ботов;
- авторизация шести Telegram-аккаунтов;
- межконтейнерный UDP на реальном Docker bridge VPS;
- реальный `sendStarsForm`, списание Stars и получение Durov’s Glasses #4444.

Поэтому локальная логика, упаковка и горячий путь прошли автоматическую проверку, но точный номер Telegram не гарантируется и боевой Stars-путь окончательно подтверждается только на VPS.
